# 01d — Setup: Environment variables

**What to build:** Create `.env.local` with `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY`. Add `.env.local` to `.gitignore`.

**Blocked by:** 01a, 01c

**Status:** ready-for-agent

- [ ] `.env.local` created with all 3 vars
- [ ] `.env.local` is in `.gitignore`
