# 01c — Setup: Install Next.js + Supabase + bcrypt dependencies

**What to build:** Ensure `@supabase/supabase-js`, `@supabase/ssr`, and `bcryptjs` are installed. Verify `npm run dev` still works.

**Blocked by:** None (can run in parallel with 01a)

**Status:** ready-for-agent

- [ ] `@supabase/supabase-js` installed
- [ ] `@supabase/ssr` installed
- [ ] `bcryptjs` installed
- [ ] `npm run dev` compiles without errors
