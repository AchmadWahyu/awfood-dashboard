# 03a — Setup: Install Vitest + config

**What to build:** Install `vitest` di devDependencies. Buat `vitest.config.ts` dengan path alias `@/` yang match tsconfig.

**Blocked by:** 01c

**Status:** ready-for-agent

- [ ] `vitest` in devDependencies
- [ ] `vitest.config.ts` — path alias `@/` points to `./src/*`
- [ ] `npm run test` added to package.json scripts
