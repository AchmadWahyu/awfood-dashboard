# 03c — Setup: Verify `npm run test` runs

**What to build:** Verifikasi bahwa `npm run test` berjalan tanpa error. Bisa dengan dummy test (`expect(1).toBe(1)`).

**Blocked by:** 03a

**Status:** ready-for-agent

- [ ] `npm run test` compiles and runs
- [ ] At least 1 passing test
