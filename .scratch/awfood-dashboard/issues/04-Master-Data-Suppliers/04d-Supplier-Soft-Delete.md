# 04d — Suppliers: Soft-delete supplier

**What to build:** Tombol toggle aktif/non-aktif di daftar supplier. Server action `softDeleteSupplier` (toggle `is_active`). Konfirmasi sebelum soft-delete.

**Blocked by:** 04a

**Status:** ready-for-agent

- [ ] Tombol toggle aktif/non-aktif per baris
- [ ] Konfirmasi dialog sebelum soft-delete
- [ ] Server action `softDeleteSupplier` — UPDATE is_active = NOT is_active
- [ ] Daftar refresh setelah toggle
