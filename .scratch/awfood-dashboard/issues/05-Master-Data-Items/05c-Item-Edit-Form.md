# 05c — Items: Form edit item

**What to build:** Form edit item (pre-filled). Server action `updateItem`. Category tidak bisa diubah setelah create (karena relasi supplier).

**Blocked by:** 05b

**Status:** ready-for-agent

- [ ] Form edit dengan data pre-filled
- [ ] Category read-only (tidak bisa diubah)
- [ ] Server action `updateItem` — UPDATE by id
- [ ] Validasi harga
