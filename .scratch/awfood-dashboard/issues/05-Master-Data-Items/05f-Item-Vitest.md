# 05f — Items: Vitest untuk server action

**What to build:** Test setiap server action items (create, read, update, soft-delete) menggunakan mock Supabase client.

**Blocked by:** 03b, 05e

**Status:** ready-for-agent

- [ ] Test `listItems` — return daftar items dengan supplier name
- [ ] Test `createItem` — INSERT, return id
- [ ] Test `updateItem` — UPDATE by id
- [ ] Test `softDeleteItem` — toggle is_active
